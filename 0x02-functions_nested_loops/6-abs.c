#include "main.h"

/**
 * _abs - Complete the absolute value of an integer
 * @t: The integer to be computed
 * Return: The absolute
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
